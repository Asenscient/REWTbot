var seconds = 0; // uptime in seconds
$.setInterval(function() {
    seconds++;
}, 1000);

$.on('command', function (event) {
    var regExp = https://nightdev.com/hosted/uptime.php?channel=leahloveschief);
    var match = url.match(regExp);
    if (match&&match[7].length==11){
        return match[7];
    }else{
        return url;
    }
    } 

});